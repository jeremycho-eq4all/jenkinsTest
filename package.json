{
  "name": "my-node-app",
  "version": "1.0.0",
  "description": "A simple Node.js application",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "mocha --reporter mocha-junit-reporter --reporter-options mochaFile=./test-results.xml",
    "build": "echo 'Building the application...' && rsync -av --progress --exclude build ./ build/"
  },
  "dependencies": {},
  "devDependencies": {
    "mocha": "^8.3.2",
    "chai": "^4.3.4",
    "chai-http": "^4.3.0",
    "mocha-junit-reporter": "^2.0.0"
  },
  "author": "A 작업자, B 작업자",
  "license": "ISC"
}
