{
  "name": "my-node-app",
  "version": "1.0.0",
  "description": "A simple Node.js application",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "mocha --reporter mocha-junit-reporter --reporter-options mochaFile=./test-results.xml",
    "build": "echo 'Building the application...' && mkdir -p build && find . -maxdepth 1 ! -name build ! -name . -exec cp -r {} build/ \\;"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "mocha": "^8.3.2",
    "chai": "^4.3.4",
    "chai-http": "^4.3.0",
    "mocha-junit-reporter": "^2.0.0"
  },
  "author": "A 작업자, B 작업자",
  "license": "ISC"
}
